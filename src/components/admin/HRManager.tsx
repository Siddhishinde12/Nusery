import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function HRManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>HR Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage employee details, attendance, and payroll here.</p>
      </CardContent>
    </Card>
  );
}
