import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SupplierManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Supplier & Purchase Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage supplier details and purchase orders here.</p>
      </CardContent>
    </Card>
  );
}
