import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function StockTaking() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Stock-Taking</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Conduct inventory audits here.</p>
      </CardContent>
    </Card>
  );
}
