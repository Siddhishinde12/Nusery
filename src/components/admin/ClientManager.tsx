import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function ClientManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Client Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage client details and history here.</p>
      </CardContent>
    </Card>
  );
}
