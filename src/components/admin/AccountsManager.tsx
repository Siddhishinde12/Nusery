import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function AccountsManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Accounts & Expenses</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage expenses, profit & loss, and ledgers here.</p>
      </CardContent>
    </Card>
  );
}
