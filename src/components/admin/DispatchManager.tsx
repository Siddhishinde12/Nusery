import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function DispatchManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Dispatch & Transport Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage order dispatches and transport details here.</p>
      </CardContent>
    </Card>
  );
}
